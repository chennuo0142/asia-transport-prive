{% extends 'base.html.twig' %}

{% block title %}ServiceListe{% endblock %}

{% block body %}
    <h1>ServiceListe</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ service_liste.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ service_liste.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ service_liste.description }}</td>
            </tr>
            <tr>
                <th>Price</th>
                <td>{{ service_liste.price }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_service_liste_index') }}">back to list</a>

    <a href="{{ path('app_service_liste_edit', {'id': service_liste.id}) }}">edit</a>

    {{ include('service_liste/_delete_form.html.twig') }}
{% endblock %}
