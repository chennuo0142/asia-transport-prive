{{ form_start(form) }}
{{ form_row(form.name)}}
{{ form_row(form.firstName)}}
{{ form_row(form.birthDay)}}
{{ form_row(form.adress)}}
{{ form_row(form.zipCode)}}
{{ form_row(form.city)}}
{{ form_row(form.country)}}
{{ form_row(form.telephone)}}
{{ form_row(form.email)}}
{{ form_row(form.language)}}
<article>
	{% if app.request.attributes.get('_route' ) == 'app_driver_edit'%}
		<img src="{{ asset('/images/uploads/' ~ driver.photo ) | imagine_filter('my_thumb_filter')}} " alt="">
	{% endif %}

	{{ form_row(form.photoImage)}}
</article>

<button class="btn">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
