{% extends 'base.html.twig' %}

{% block title %}Edit Driver
{% endblock %}

{% block body %}
	<div class="space-between">
		<h1>Edit Driver</h1>
		<span>Etat:
			{{ status }}</span>
	</div>
	{{ include('driver/_form.html.twig', {'button_label': 'Update'}) }}

	<a href="{{ path('app_driver_index') }}">back to list</a>

	{{ include('driver/_delete_form.html.twig') }}
{% endblock %}
