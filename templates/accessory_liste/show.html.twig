{% extends 'base.html.twig' %}

{% block title %}AccessoryListe{% endblock %}

{% block body %}
    <h1>AccessoryListe</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ accessory_liste.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ accessory_liste.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ accessory_liste.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_accessory_liste_index') }}">back to list</a>

    <a href="{{ path('app_accessory_liste_edit', {'id': accessory_liste.id}) }}">edit</a>

    {{ include('accessory_liste/_delete_form.html.twig') }}
{% endblock %}
